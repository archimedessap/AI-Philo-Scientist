```json
{"derivation": "### Step 1: Analyze the Applicability of PIR's Novel Dynamics\n\nThe theory, Path Integral Realism (PIR), is presented as an **extension** to Standard Quantum Mechanics (SQM). A crucial part of evaluating an extension is determining the regime in which its predictions deviate from the theory it extends. PIR's documentation specifies its novel effects arise from the 'Dynamic Actualization' process governed by the Actuality Operator, `A`. The primary testable deviation, 'Gravitationally-Modulated Historical Decoherence', requires specific conditions: a **mesoscopic system**, **long interrogation times**, and **modulated gravitational fields** to make the 'consistency metric' `g_C` between paths non-trivial and dynamic.\n\nThe specified experiment, `wheeler_delayed_choice_jacques2007`, uses **single photons**. Photons are massless, relativistic, and quintessentially microscopic particles. The experiment does not involve large masses or modulated gravitational fields. Therefore, it falls squarely within the domain where PIR's novel dynamics are predicted to be negligible.\n\nAs stated in the theory's documentation under `reproduces_sqm_predictions`: *\"For microscopic systems, the evolution of A is too slow to be detected in typical experiments, so standard unitary evolution and the path integral formalism of SQM are recovered as an excellent approximation.\"* Consequently, the derivation must proceed by applying the standard quantum formalism, which PIR subsumes in this limit.\n\n### Step 2: Formulate the State within the PIR Framework\n\nAccording to PIR, the state of the system is a vector `|Ψ_path⟩` in the Path-State Space `H_PS`. After the photon passes the first beam splitter of the Mach-Zehnder interferometer, the Potentiality Field is a superposition of two bundles of histories:\n\n`|Ψ_path⟩ = (1/√2) (|h_upper⟩ + e^(iφ) |h_lower⟩)`\n\nHere, `|h_upper⟩` represents the bundle of histories where the photon traverses the upper arm, `|h_lower⟩` represents the bundle where it traverses the lower arm, and `φ` is the relative phase difference between them. This formulation is isomorphic to the standard Hilbert space description in this simple case.\n\n### Step 3: Analyze the 'Delayed Choice' Measurement\n\nThe experiment's core is the delayed choice of whether to insert the second beam splitter (BS2). We are interested in the `fringe_visibility`, which is only meaningful when the choice is made to observe interference (i.e., BS2 is inserted).\n\nWhen BS2 is inserted, the two paths are recombined. In the standard formalism, the action of BS2 on the path states `|h_upper⟩` and `|h_lower⟩` is:\n\n`BS2 |h_upper⟩ → (1/√2) (|D1⟩ + |D2⟩)`\n`BS2 |h_lower⟩ → (1/√2) (|D1⟩ - |D2⟩)`\n\nwhere `|D1⟩` and `|D2⟩` are states corresponding to a detection at detector 1 and 2, respectively.\n\nApplying this transformation to our state `|Ψ_path⟩` yields the final state before detection:\n\n`|Ψ_final⟩ = (1/2) [ (|D1⟩ + |D2⟩) + e^(iφ) (|D1⟩ - |D2⟩) ]`\n`|Ψ_final⟩ = (1/2) [ (1 + e^(iφ))|D1⟩ + (1 - e^(iφ))|D2⟩ ]`\n\n### Step 4: Calculate Detection Probabilities and Fringe Visibility\n\nThe probability of detecting the photon at detector D1 is the squared magnitude of the amplitude for `|D1⟩`:\n\n`P(D1) = |(1/2)(1 + e^(iφ))|² = (1/4) |1 + cos(φ) + i sin(φ)|²`\n`P(D1) = (1/4) [ (1 + cos(φ))² + sin²(φ) ] = (1/4) [ 1 + 2cos(φ) + cos²(φ) + sin²(φ) ]`\n`P(D1) = (1/4) [ 2 + 2cos(φ) ] = (1/2)(1 + cos(φ)) = cos²(φ/2)`\n\nThis probability represents the intensity `I` at the detector. The intensity varies with the phase `φ`.\n- The maximum intensity, `I_max`, occurs when `cos(φ) = 1`, so `I_max = 1`.\n- The minimum intensity, `I_min`, occurs when `cos(φ) = -1`, so `I_min = 0`.\n\nFringe visibility `V` is defined as:\n\n`V = (I_max - I_min) / (I_max + I_min)`\n\nSubstituting the calculated values:\n\n`V = (1 - 0) / (1 + 0)`\n\n### Step 5: Final Prediction\n\nThe calculation yields a theoretical fringe visibility of 1. \n\n### Justification from PIR's Principles:\nThis result is a direct consequence of PIR's explicit statement that it reproduces SQM for microscopic systems. The PIR ontology provides a narrative: the 'Potentiality Field' `|Ψ_path⟩` evolves unitarily. The measurement setting (BS2 inserted) defines a basis of possible outcomes (e.g., 'detection at D1' vs 'detection at D2'). The 'Dynamic Actualization' process, governed by the Actuality Operator `A`, then selects one of these outcomes. Because the system is microscopic and the experiment is fast, the non-linear evolution of `A` is negligible. Therefore, the probabilities of `A` selecting a given outcome are identical to the probabilities given by the Born rule applied to the final state `|Ψ_final⟩`. The theory's complex machinery defaults to the standard, empirically verified quantum predictions in this regime, which is a necessary condition for its viability."}
```
```json
{"value": 1}
```