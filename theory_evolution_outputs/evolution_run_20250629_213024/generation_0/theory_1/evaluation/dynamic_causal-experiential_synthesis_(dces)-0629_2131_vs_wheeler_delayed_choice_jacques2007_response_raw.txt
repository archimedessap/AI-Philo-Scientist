```json
{"derivation": "### 1. Analysis of the Experiment under DCES\n\nThe Wheeler's Delayed Choice experiment is analyzed within the Dynamic Causal-Experiential Synthesis (DCES) framework not as a particle or wave propagating through pre-existing spacetime, but as a process of graph growth governed by probabilistic laws. \n\n- **Photon as a Process:** The 'single photon' is not an object but a chain of 'actualization events' (nodes) in the Causal-Experiential Graph (CEG). Its creation is an event `v_source`.\n- **Interferometer as a Subgraph:** The Mach-Zehnder interferometer is a stable, pre-existing subgraph of the CEG.\n- **Propagation as Amplitude Evolution:** When the photon process interacts with the first beam splitter (`BS_in`), it doesn't split. Instead, the 'Graph Wavefunction' `Ψ`, representing the potential for future actualizations, propagates along two distinct sets of potential causal links (edges) corresponding to the interferometer arms (Path A and Path B). This follows the `Recursive Amplitude Propagation` equation: `Ψ(v_j) = \\sum_{v_i \\in \\text{parents}(v_j)} A(v_i, v_j) Ψ(v_i)`.\n- **Delayed Choice as Agency:** The experimenter's choice to insert or remove the output beam splitter (`BS_out`) is an act of agency as described in `Postulate 5`. This choice determines the structure of the 'Potential Frontier' of the CEG. It dictates which set of potential future actualization events (detector clicks) are available to be connected to the prior events of the photon's propagation.\n- **Measurement as Actualization:** A detector click is the final 'actualization event' (`v_det`) in this experimental sequence. According to `Postulate 1`, this event is a fundamental act of 'becoming'. The structure of the graph between `v_source` and `v_det` is only fixed once `v_det` is actualized. The paradox is resolved because there is no fact of the matter about which path the photon 'took' until a measurement actualizes a specific outcome, which retroactively defines the realized causal path through the graph.\n\n### 2. Derivation of Fringe Visibility\n\nDCES is an extension of SQM that is designed to reproduce its predictions in standard, well-isolated experimental contexts (`reproduces_sqm_predictions`). The novel prediction of `Interaction-Density Dependent Decoherence` is not triggered here. We therefore use the DCES formalism, which converges to the standard quantum prediction.\n\nThe target observable is `fringe_visibility`, which requires analyzing the 'wave' configuration where `BS_out` is inserted.\n\n**Step 1: Initial State**\nLet the actualization event at the source be `v_S`. We normalize the initial amplitude: `Ψ(v_S) = 1`.\n\n**Step 2: First Beam Splitter (`BS_in`)**\n`BS_in` creates two potential pathways in the CEG. Let the first potential nodes along Path A and Path B be `v_A` and `v_B`. For a 50/50 beam splitter, the amplitudes `A` for these links are `1/\\sqrt{2}` (transmission) and `i/\\sqrt{2}` (reflection). \n`Ψ(v_A) = A(v_S, v_A)Ψ(v_S) = \\frac{1}{\\sqrt{2}}`\n`Ψ(v_B) = A(v_S, v_B)Ψ(v_S) = \\frac{i}{\\sqrt{2}}`\n\n**Step 3: Path Propagation and Phase Shift**\nAmplitudes propagate along the potential paths. We introduce a controllable phase shift `φ` in Path B. The amplitudes at the potential nodes just before `BS_out`, `v'_A` and `v'_B`, are:\n`Ψ(v'_A) = Ψ(v_A) = \\frac{1}{\\sqrt{2}}`\n`Ψ(v'_B) = e^{iφ}Ψ(v_B) = \\frac{i e^{iφ}}{\\sqrt{2}}`\n\n**Step 4: Second Beam Splitter (`BS_out`) and Detectors**\nWith `BS_out` inserted, the potential actualization events at the detectors, `v_{D1}` and `v_{D2}`, are each connected to both `v'_A` and `v'_B`. We apply the `Recursive Amplitude Propagation` law to find the total amplitude at each potential detector event.\n\nFor Detector 1 (`v_{D1}`):\n`Ψ(v_{D1}) = A(v'_A, v_{D1})Ψ(v'_A) + A(v'_B, v_{D1})Ψ(v'_B)`\nUsing standard beam splitter matrix elements (`A_{trans} = 1/\\sqrt{2}`, `A_{refl} = i/\\sqrt{2}`):\n`Ψ(v_{D1}) = (\\frac{1}{\\sqrt{2}}) (\\frac{1}{\\sqrt{2}}) + (\\frac{i}{\\sqrt{2}}) (\\frac{i e^{iφ}}{\\sqrt{2}}) = \\frac{1}{2} (1 - e^{iφ})`\n\n**Step 5: Probabilistic Actualization**\nThe probability of an actualization event at Detector 1 is given by the `Probabilistic Actualization (Born-Growth Law)`: `P(v_{D1}) \\propto ||Ψ(v_{D1})||^2`.\n`P(v_{D1}) \\propto ||\\frac{1}{2} (1 - e^{iφ})||^2 = \\frac{1}{4} |1 - \\cos(φ) - i\\sin(φ)|^2`\n`P(v_{D1}) \\propto \\frac{1}{4} ((1-\\cos(φ))^2 + \\sin^2(φ)) = \\frac{1}{4} (1 - 2\\cos(φ) + \\cos^2(φ) + \\sin^2(φ)) = \\frac{1}{2}(1 - \\cos(φ))`\nThis equation describes the interference fringe pattern as a function of the phase `φ`.\n\n**Step 6: Fringe Visibility Calculation**\nFringe visibility `V` is defined as `V = (I_max - I_min) / (I_max + I_min)`, where intensity `I` is proportional to probability `P`.\n- Maximum intensity `I_max` occurs when `\\cos(φ) = -1`, so `P_{max} \\propto \\frac{1}{2}(1 - (-1)) = 1`.\n- Minimum intensity `I_min` occurs when `\\cos(φ) = 1`, so `P_{min} \\propto \\frac{1}{2}(1 - 1) = 0`.\n\nTherefore, the theoretical fringe visibility is:\n`V = \\frac{1 - 0}{1 + 0} = 1`\n\n### 3. Justification\nThis derivation is a direct consequence of the DCES framework. It relies on the `Amplitude Postulate` for the existence of complex amplitudes on potential causal links, the `Recursive Amplitude Propagation` equation to sum these amplitudes over different potential paths, and the `Probabilistic Actualization` law (the Born-Growth Law) to convert the final amplitude into a probability for a physical event. The experimenter's role in setting up the measurement apparatus is physically grounded in the `Agency Postulate`, which states that the agent's choices influence the set of potential events considered for actualization. The theory successfully reproduces the standard quantum mechanical prediction, as expected from its `comparison_with_sqm` section, because the specific conditions for deviations are not met in this experiment."}
```
```json
{"value": 1.0}
```