---
description: 论文写作与素材导出规范（自动化生成图表/表格，增量编译PDF）
alwaysApply: false
---

# 论文协作规则（供 Agent 与 Inline Edit 使用）
- 图与表输出目录：图放 `paper/figs/`，表放 `paper/tables/`；论文用 `\includegraphics{figs/...}` 与 `\input{tables/...}` 引用。
- 生成素材：
  - 生成图：`python3 scripts/gen_figs.py paper/figs`
  - 生成表：`python3 scripts/gen_tables.py paper/tables`
- 编译论文：
  - 快速构建：`bash scripts/build_paper.sh` 或 `make paper`
  - 仅编译：`cd paper && latexmk -pdf -interaction=nonstopmode -file-line-error ai-philo-crossAI-arxiv.tex`
- 审核流程：
  - 修改实验脚本或结果后，先运行最小化脚本（如有）或 `python -m pytest -q` 验证；再生成图表并编译PDF。
  - 引用统一用 `\label{}` + `\cref{}`（cleveref），避免硬编码章节/图表编号。
- 参考文献来源：仅允许引用 `paper/ai-philo-refs.bib` 中条目，不得编造。

